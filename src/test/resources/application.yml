session-couchbase:
  timeout-in-seconds: 1800
  persistent:
    namespace: default
    json-serialization: false
    hosts: localhost
    bucket-name: default
    password:
  inMemory:
    enabled: false